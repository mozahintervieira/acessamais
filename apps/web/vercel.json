{
  "framework": "nextjs",
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "buildCommand": "cd ../.. && pnpm db:generate && pnpm db:deploy:production && pnpm turbo build --filter=@acessa-plus/web...",
  "outputDirectory": ".next"
}
