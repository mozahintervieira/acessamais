{
  "framework": "nextjs",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm db:generate && pnpm turbo build --filter=@acessa-plus/web...",
  "outputDirectory": "apps/web/.next"
}
